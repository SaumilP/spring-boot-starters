spring:
  application:
    name: rate-limiting-example
  data:
    redis:
      host: localhost
      port: 6379
  rate-limit:
    named-limits:
      admin-search:
        requests: 100
        window-seconds: 60
